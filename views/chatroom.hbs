    <div id="title-section">
      <a href="/"><img src="{{image}}" alt="touchat"></a>
    </div>
    <div id ="video-area">
      

      <div id ="control-area">
        <!-- BUTTON CONTROLS TO BE ADDED
        BUTTON FOR VIDEO, AUDIO, AND LINK TO /CHATROOM/{{TITLE}}
        -->
      </div>

    </div>
    <div id="chat-area">
      <div id="chat-title">Chat <button id="chatState" ><i class="arrow down"></i></button></div>
      {{name}}
    <ul id="messages"></ul>
    <form id="form" action="">
      <input id="input" autocomplete="off" placeholder="Type message here"/><button id="send-button">Send</button>
    </form>
    </div>
     <div id="chat-down">Chat <button id="chatState" ><i class="arrow up"></i></button></div>

    <script src="script.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
      const ROOM_ID = "{{title}}"
       var width = screen.width;

       
      if (width >= '800px') { 
        document.getElementById('chat-area').style.display='block'; 
      }
    
    //changing desktop to mobile state
      var width = $(window).width();
      $(window).resize(function(){
      if($(this).width() != width){
          width = $(this).width();
          if(width>800){
          $('#chat-area').show();
          $('#chat-down').hide();
          }
          if(width<=800){
          $('#chat-area').hide();
          $('#chat-down').show();
          }
        }
      });
      //closing chat
      $("#chatState").click(function(){
        $('#chat-area').hide();
        $('#chat-down').show();
      });
      //opening chat
      $("#chat-down").click(function(){
        $('#chat-area').show();
        $('#chat-down').hide();
      });

      
    </script>
    

    

